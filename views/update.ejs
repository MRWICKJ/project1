<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="text-white" style="background-color: #111;">
<div class="absolute inset-0 flex justify-center items-center">
    <div class="container w-1/4 mx-auto mt-10 bg-zinc-800 p-6 rounded-lg">
        <h1 class="text-2xl font-semibold mb-5 text-blue-500">Update User</h1>
        <form action="/api/update" method="POST">
            <div class="mb-4">
                <label class="block text-emerald-700 text-sm font-bold mb-2" for="username">
                    Old Email
                </label>
                <input class="bg-neutral-800 text-emerald-400 shadow appearance-none border rounded w-full py-2 px-3 leading-tight focus:outline-none focus:shadow-outline" name="oldEmail" type="email" placeholder="Enter Your old Email" required>
            </div>
            <div class="mb-6">
                <label class="block text-emerald-700 text-sm font-bold mb-2" for="email">
                    New Email
                </label>
                <input class="bg-neutral-800 text-emerald-400 shadow appearance-none border rounded w-full py-2 px-3 leading-tight focus:outline-none focus:shadow-outline" name="newEmail" type="email" placeholder="Enter Your new email" required>
            </div>
            <div class="flex items-center justify-between">
                <button class="bg-yellow-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit">
                    Update User
                </button>
            </div>
        </form>
        <a href="/" class="text-blue-500">Return Home</a>
        <% if (name) { %>
            <p>Name: <%= name %></p>
            <p>Username: <%= username %></p>
            <p>Email: <%= email %></p>
        <% } else { %>
            <p>User not found.</p>
        <% } %>
    </div>
</div>
</body>

</html>
